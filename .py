import os
from time import sleep
from random import randint

"""def clearConsole():
    command = 'clear'
    if os.name in ('nt', 'dos'):  # If Machine is running on Windows, use cls
        command = 'cls'
    os.system(command)

sla = 'Olá caro soldado, tranquis. Vamos começar?'
Kk = []

for i in range(len(sla) - 1):
    Kk.append(sla[i])
    print(''.join(Kk))

    while len(Kk) == len(sla) - 1:
        i = str(input('? '))

        if i == 'sla':
            clearConsole()
            break

        else:
            pass

    else:
        sleep(0.1)

        clearConsole()"""


sla = eval('5 + 5')

print(int(sla))